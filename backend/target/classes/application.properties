server.port=8080

# Default profile: H2 (local dev). Set SPRING_PROFILES_ACTIVE=prod for a real DB.
spring.profiles.active=${SPRING_PROFILES_ACTIVE:default}

# JPA (dialect and ddl are set per profile)
spring.jpa.defer-datasource-initialization=true
